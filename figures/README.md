# Figures

source contains the nine original Draw.io files. Their contents were not
modified during renaming.

published contains exact author exports from the final manuscript source.
These are useful visual references when checking regenerated plots.

generated is populated by python analysis/generate_all.py. Data-driven plots
are rebuilt from CSV files. Hand-drawn panels are materialized from the
verified author exports because Draw.io export is an interactive tool step.
